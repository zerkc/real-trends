import { observer } from "mobx-react-lite";
import React from "react";



export default observer((props)=>{
    return (<div>
            <input />
            <button>Add</button>
        </div>);
})